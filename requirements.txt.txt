# ============================================================
# Insurance Retention Analytics — Python Dependencies
# Python 3.11+
#
# IMPORTANTE: versiones pinneadas para evitar conflictos entre
# numpy 1.x ↔ 2.x. SHAP 0.44.x es la última versión compatible
# con numpy 1.26.x (el estándar de Anaconda).
# ============================================================

# --- Core analytics ---
pandas>=2.0.0,<3.0
numpy>=1.24.0,<2.0       # CRITICAL: mantener en 1.x para compatibilidad con Anaconda
scipy>=1.10.0,<1.14

# --- Visualization ---
matplotlib>=3.7.0,<4.0
seaborn>=0.12.0,<0.14

# --- Machine Learning ---
scikit-learn>=1.3.0,<1.5
xgboost>=2.0.0,<3.0      # CRITICAL: xgboost 3.x rompe SHAP 0.44 (formato base_score cambió)

# --- Model interpretability ---
shap>=0.44.0,<0.46       # 0.46+ exige numpy 2.x → rompería Anaconda

# --- Notebook environment ---
jupyterlab>=4.0.0
ipykernel>=6.25.0

# --- SQL Server connectivity (opcional) ---
pyodbc>=5.0.0
sqlalchemy>=2.0.0

# --- Almacenamiento eficiente (Parquet) ---
pyarrow>=14.0.0,<16.0

# --- Utilidades ---
joblib>=1.3.0
python-dotenv>=1.0.0
